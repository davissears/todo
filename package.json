{
  "name": "todo",
  "module": "src/index.js",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@happy-dom/global-registrator": "^20.8.9",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run ./dev.ts --hot",
    "build": "bun ./build.ts",
    "deploy": "bun run build && bunx wrangler deploy",
    "test:unit": "bun test --dom tests/unit"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
